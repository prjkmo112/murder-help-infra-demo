# docker-compose.yaml 로 띄운 mysql/redis 를 사용하는 로컬 개발용 설정
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/murder_help
    username: murder
    password: murder1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
